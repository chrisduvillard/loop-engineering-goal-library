name: Apply interactive-first updates

on:
  push:
    branches: [codex/interactive-shaping-first]

concurrency:
  group: interactive-first-update
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out review branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: codex/interactive-shaping-first
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Generate and verify
        shell: bash
        run: |
          set -euo pipefail
          python scripts/sync_goal_launchers.py --write
          python scripts/sync_goal_docs.py --write
          rm -f .github/workflows/apply-interactive-first.yml
          python scripts/sync_goal_launchers.py --check
          python scripts/sync_goal_docs.py --check
          python scripts/validate_shaping_history_diff.py --self-test --base-ref origin/main
          python scripts/validate_repository.py
          python scripts/package_skills.py
          output="$(npx -y skills@1.5.23 add . --list)"
          printf '%s\n' "$output"
          grep -q "shape-goal" <<<"$output"
          grep -q "goal-engine" <<<"$output"

      - name: Commit generated files
        shell: bash
        run: |
          set -euo pipefail
          git checkout HEAD -- .github/workflows/apply-interactive-first.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Synchronize interactive-first profiles and docs"
          git push origin HEAD:codex/interactive-shaping-first
