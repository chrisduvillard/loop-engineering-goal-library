name: Apply second-pass adversarial refinements

on:
  push:
    branches: [codex/adversarial-robustness-review]

concurrency:
  group: second-pass-adversarial-robustness
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out review branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: codex/adversarial-robustness-review
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Set up Node
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903 # v6.0.0
        with:
          node-version: "22.20.0"
          cache: npm

      - name: Apply and verify
        shell: bash
        run: |
          set -euo pipefail
          python scripts/fix_refinement_script.py
          python scripts/refine_adversarial_robustness.py
          python scripts/post_refinement_fix.py
          rm -f scripts/fix_refinement_script.py scripts/refine_adversarial_robustness.py scripts/post_refinement_fix.py
          mv .github/workflows/apply-second-pass-robustness.yml /tmp/apply-second-pass-robustness.yml
          npm ci --ignore-scripts
          python -m compileall -q scripts tests
          python -m unittest discover -s tests -v
          python scripts/sync_goal_launchers.py --check
          python scripts/sync_goal_docs.py --check
          python scripts/validate_shaping_history_diff.py --self-test --base-ref origin/main
          python scripts/validate_repository.py
          output="$(npx --no-install skills add . --list)"
          printf '%s\n' "$output"
          grep -q "shape-goal" <<<"$output"
          grep -q "goal-engine" <<<"$output"
          python scripts/package_skills.py
          rm -rf node_modules
          mv /tmp/apply-second-pass-robustness.yml .github/workflows/apply-second-pass-robustness.yml

      - name: Commit reviewed refinements
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Close second-pass adversarial findings"
          git push origin HEAD:codex/adversarial-robustness-review
