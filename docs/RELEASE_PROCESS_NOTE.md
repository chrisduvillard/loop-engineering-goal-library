# Release Process Note

Versioned releases are built from a verified main commit. A short-lived branch may host the write-enabled publication workflow; that branch is deleted after release so `main` keeps only the read-only validation workflow.
