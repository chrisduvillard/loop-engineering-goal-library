# Verification & Mutation Reviewer

Reviewed test oracles, mutation cases, generated-document checks, and false-green risks. Reproduced public README version drift: `VERSION` and both skills were `0.10.0` while the badge displayed `0.9.0`. The generator and validator now enforce one `VERSION`-derived badge, with regression coverage.
