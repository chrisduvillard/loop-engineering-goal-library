# Merge CI Gate

The pull request must pass `Validate library` before merge. The gate includes cross-platform tests, canonical state controls, generated-source synchronization, package closure, and behavioral self-tests.
