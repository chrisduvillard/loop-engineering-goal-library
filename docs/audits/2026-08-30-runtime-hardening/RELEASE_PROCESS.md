# Release Publication Process

The 0.12.0 release is built from the verified `main` commit with deterministic packages, checksums, and a release manifest. A short-lived write-enabled workflow branch is deleted after publication.
