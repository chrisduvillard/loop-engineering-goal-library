# Why version 0.11.1 remains beta

The repository can prove deterministic files, transitions, leases, package
closure, mutation boundaries, verifier outcomes, and release reproducibility.
It cannot independently authenticate into Codex or Claude Code and force their
native context-compaction behavior. The beta label therefore reflects one
specific evidence boundary, not an unfinished repository implementation.
