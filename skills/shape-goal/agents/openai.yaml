interface:
  display_name: "Shape Goal"
  short_description: "Discover missing inputs and approve a safe Goal Contract."
  default_prompt: "Inspect this repository, resolve the next goal's missing inputs from evidence, ask only material owner decisions, and produce an approved Goal Contract."
policy:
  allow_implicit_invocation: true
